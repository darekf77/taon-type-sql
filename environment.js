
const path = require('path')
    var { config } = { config: {} };

    config = {

      domain: 'taon-type-sql.example.domain.com',
      useDomain: false,
      title: 'Taon Type Sql',
      pwa: {
        // start_url: ''
      }

    }
    module.exports = exports = { config };
